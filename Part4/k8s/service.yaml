apiVersion: v1
kind: Service
metadata:
  name: cats-dogs-api-service
  labels:
    app: cats-dogs-api
spec:
  type: NodePort
  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 30080
    protocol: TCP
  selector:
    app: cats-dogs-api
